---
import ExamplesHome from "../components/ExamplesHome";
import Layout from "../layouts/Layout.astro";
import { aggregateBoards } from "../lib/boards";
import { getAllExamples } from "../lib/examples";

const examples = getAllExamples();
const boards = aggregateBoards(examples);

if (import.meta.env.DEV) {
  console.log("[examples]", examples);
  console.log("[boards]", boards);
}
---

<Layout title="RuyiSDK Examples">
  <ExamplesHome client:load examples={examples} boards={boards} />
</Layout>
